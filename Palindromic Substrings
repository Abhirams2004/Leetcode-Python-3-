class Solution:
    def countSubstrings(self, s: str) -> int:
        c=0
        l=[]
        for i in range(len(s)):
            for j in range(i+1,len(s)+1):
                l.append(s[i:j])
        for i in l:
            if(i==i[::-1]):
                c+=1
        return c
        
